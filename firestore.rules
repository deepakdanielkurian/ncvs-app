rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Custom auth — all reads/writes open (login checked in app code)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
